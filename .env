# ===============================================
# Environment Variables for Docker Compose
# ===============================================

# --- PostgreSQL Database Configuration ---
# These are used by the 'db' service in docker-compose.
POSTGRES_DB=proctoraegis_db
POSTGRES_USER=proctoraegis_user
POSTGRES_PASSWORD=supersecretpassword

# --- Adminer Configuration (Database GUI) ---
# This tells the Adminer GUI which database to connect to by default.
ADMINER_DEFAULT_SERVER=db

# --- Backend Application Configuration ---
# This is the variable your Python app is crashing for.
# It's the full connection string, built from the variables above.
# The hostname 'db' is used because the containers are on the same Docker network.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# This is for the CORS middleware in your main.py.
# It tells the backend to accept requests from your frontend's address.
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]
